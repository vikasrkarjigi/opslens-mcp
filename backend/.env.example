# Copy to .env to enable real Claude reasoning. Without it the system runs in mock mode.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Set to 1 to talk to the MCP server over stdio instead of in-process.
MCP_USE_SUBPROCESS=0
